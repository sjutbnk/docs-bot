import json


EXTRACTION_PROMPT = """
Извлеки данные из приложенных документов (паспорт, патент на работу РФ и, при наличии, Карта партнёра / реквизиты работодателя).

Верни ОДИН плоский JSON-объект (без вложенности, без разделов, все ключи на одном уровне) со следующими ключами:

Данные иностранного гражданина:
- full_name         (ФИО полностью на русском, например: Жобборов Хамид Исмоилович)
- citizenship       (гражданство, например: Узбекистан)
- birth_date        (дата рождения в формате ДД.ММ.ГГГГ)
- passport_series   (серия паспорта, например FA)
- passport_number   (номер паспорта, только цифры)
- passport_issue_date (дата выдачи паспорта в формате ДД.ММ.ГГГГ)
- passport_issued_by  (кем выдан — полное наименование органа)
- address           (адрес регистрации/проживания; пустая строка если нет)
- patent_series     (серия патента, обычно 2 цифры, например 30)
- patent_number     (номер патента, 10 цифр)
- patent_issue_date (дата выдачи патента в формате ДД.ММ.ГГГГ)
- patent_expiry_date (дата окончания действия патента в формате ДД.ММ.ГГГГ)
- profession        (профессия в патенте; если нет — пиши Овощевод)
- inn               (ИНН налогоплательщика ровно 12 цифр; если нет — пустая строка)

Данные Работодателя (только если есть Карта партнёра):
- employer_type     (строго "ИП" или "ЮрЛицо"; пустая строка если нет Карты)
- employer_name     (полное наименование работодателя; пустая строка если нет Карты)
- employer_inn      (ИНН работодателя; пустая строка если нет Карты)
- employer_ogrn     (ОГРН или ОГРНИП; пустая строка если нет Карты)
- employer_address  (юридический адрес работодателя; пустая строка если нет Карты)
- employer_passport_series  (серия паспорта ИП; пустая строка если нет или ЮрЛицо)
- employer_passport_number  (номер паспорта ИП; пустая строка если нет или ЮрЛицо)
- employer_passport_issue_date (дата выдачи паспорта ИП ДД.ММ.ГГГГ; пустая строка если нет)
- employer_passport_issued_by  (кем выдан паспорт ИП; пустая строка если нет)
- work_address      (место осуществления деятельности / адрес работы; пустая строка если нет)
- foreigner_registration_address (адрес регистрации ИРС; пустая строка если нет)
- employer_rs       (расчетный счет р/с, 20 цифр; пустая строка если нет)
- employer_ks       (корреспондентский счет к/с, 20 цифр; пустая строка если нет)
- employer_bik      (БИК, 9 цифр; пустая строка если нет)
- employer_bank     (наименование банка и отделения; пустая строка если нет)

ВАЖНО:
1. JSON должен быть ПЛОСКИМ — никаких вложенных объектов и массивов!
2. Если данных нет — ставь пустую строку "".
3. Никакого текста кроме валидного JSON!

Пример правильного формата:
{"full_name": "Иванов Иван Иванович", "citizenship": "Узбекистан", "employer_name": "", ...}
"""


def get_validator_prompt(initial_data: dict) -> str:
    return f"""
Ниже представлен JSON с данными, извлечёнными из фото документов:
{json.dumps(initial_data, ensure_ascii=False, indent=2)}

Твоя задача — выступить в роли строгого Аудитора:
1. Сверь каждую букву и цифру в JSON с приложенными фото. Исправь все опечатки (в ФИО, датах, номерах).
2. Приведи full_name к правильному регистру: первые буквы заглавные, остальные строчные. Пример: Кенжаев Бахром Хуррамович.
3. Приведи citizenship к читаемому виду (например: Узбекистан, Таджикистан).
4. Убедись, что inn содержит ровно 12 цифр — если нет, сбрось в пустую строку.
5. Убедись, что все даты в формате ДД.ММ.ГГГГ.
6. Не удаляй ни одного ключа, даже если значение пустое.

Верни строго финальный, проверенный JSON с теми же самыми ключами. Никакого лишнего текста!
"""
